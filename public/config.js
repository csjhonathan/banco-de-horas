// URL base da API. Vazio = mesma origem (front servido pelo próprio backend).
// Ao hospedar o front separado (ex.: GitHub Pages), aponte para o backend:
//   window.API_URL = "https://seu-backend.onrender.com";
// Quando servido pelo backend deste projeto, o server sobrescreve este arquivo
// com o valor da env API_URL.
window.API_URL = "";
